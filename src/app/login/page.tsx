import { TopNav } from "_components/TopNav"

export default function LoginPage() {
    // will need some logic here for login - also what are we using as db

  return (
    <>
    <TopNav></TopNav>
    <div>
        This is the login page. We will have a sign up option here.
        Can probably use the same component for that - one get one post
    </div>
    </>
  );
}
